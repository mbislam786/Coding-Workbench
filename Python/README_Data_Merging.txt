1- Setup your environment with necessary imports like pandas
2- The provided python file(.py) containing code snippets were executed using VSCode 
3- Loaded both the files (Sales_January_2019.csv and Sales_February_2019.csv) and including the relative month as the column inside the each file. 
4- Merged both files using concat() method because the files had same structure. You can use merge method if the files do not have same structure and hence can be merged based on a common column. 
5- Duplicate values were removed using Order_ID column.
6- Null value were removed completely based on important columns completely. (Based on needs or tasks you can replace the null values for example with average or anything suitable for your task for any particular column if you want to.)
7- Saved the output as the excel file. 